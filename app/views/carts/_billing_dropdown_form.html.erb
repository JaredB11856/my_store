<%= form_for(billing_information) do |f| %>
  <% if billing_information.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(billing_information.errors.count, "error") %> prohibited this billing_information from being saved:</h2>

      <ul>
      <% billing_information.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label "Status", class: 'control-label' %>
    <div class="controls">
      <%= f.collection_select :billing_information_id, BillingInformation.all, :billing_information_id, :address_line_1, prompt: true %>
      <%= f.submit "Proceed", class: 'btn btn-primary btn-block' %>      
      
    </div>
    <%= ShippingAddress.all.ids.inspect %>
  </div>

<% end %>