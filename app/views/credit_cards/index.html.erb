<p id="notice"><%= notice %></p>

<h1>Credit Cards</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Expiration mm</th>
      <th>Expiration yy</th>
      <th>Card number</th>
      <th>Cvc</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @credit_cards.each do |credit_card| %>
      <tr>
        <td><%= credit_card.name %></td>
        <td><%= credit_card.expiration_mm %></td>
        <td><%= credit_card.expiration_yy %></td>
        <td><%= credit_card.card_number %></td>
        <td><%= credit_card.cvc %></td>
        <td><%= link_to 'Show', credit_card %></td>
        <td><%= link_to 'Edit', edit_credit_card_path(credit_card) %></td>
        <td><%= link_to 'Destroy', credit_card, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Credit Card', new_credit_card_path %>
